{
  "name": "@bubblav/strapi-plugin-bubblav",
  "version": "0.1.0",
  "description": "Connect your Strapi content to BubblaV, the AI chatbot for your website. Adds a Settings page that walks you through creating a read-only API token and opens the BubblaV connect flow.",
  "keywords": [
    "strapi",
    "strapi-plugin",
    "chatbot",
    "bubblav"
  ],
  "license": "MIT",
  "author": "BubblaV <support@bubblav.com> (https://www.bubblav.com)",
  "homepage": "https://www.bubblav.com?utm_source=strapi-market",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bubblav-org/strapi-plugin-bubblav.git"
  },
  "bugs": {
    "url": "https://github.com/bubblav-org/strapi-plugin-bubblav/issues"
  },
  "type": "commonjs",
  "main": "./strapi-server.js",
  "exports": {
    "./package.json": "./package.json",
    "./strapi-admin": "./strapi-admin.js",
    "./strapi-server": "./strapi-server.js"
  },
  "files": [
    "dist",
    "server",
    "strapi-admin.js",
    "strapi-server.js",
    "README.md",
    "LICENSE"
  ],
  "strapi": {
    "name": "bubblav",
    "kind": "plugin",
    "displayName": "BubblaV",
    "description": "Setup helper for the BubblaV AI chatbot: token setup guide and deep link into the BubblaV connect flow."
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=18.0.0 <=24.x.x",
    "npm": ">=6.0.0"
  },
  "dependencies": {
    "@strapi/design-system": "^2.0.0",
    "@strapi/icons": "^2.0.0"
  },
  "peerDependencies": {
    "@strapi/strapi": "^5"
  },
  "scripts": {
    "build": "esbuild admin/src/index.js --bundle --outfile=dist/admin.js --platform=browser --format=cjs --jsx=automatic --external:react --external:react/* --external:react-dom --external:react-dom/* --external:react-router-dom --external:@strapi/*",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
